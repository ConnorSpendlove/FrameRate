<div id = "cardCenterer"class="col s12 m4">
  <div id = "darkRedProfileCard" class="card red darken-4">
    <div class="card-content white-text">
      <span id ="reviewTitle"class="card-title">Your Reviews</span>
      {{#each profiles as |profile| }}
        {{> profileinfo}}
      {{/each}}
    </div>
  </div>
</div>
<script>
  // Function to fetch new review data and render it
  async function renderNewReview() {
    try {
      // Fetch new review data from the server
      const response = await fetch('/api/user/game', {
        method: 'POST',
      });
    
      const newReviewData = await response.json();
    } catch (error) {
  // Code to handle the error
} finally {
  // Optional code that runs regardless of whether there was an error or not
}
    
  }
  // Call the renderNewReview function to render the initial reviews
  renderNewReview();
</script>


